rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite que qualquer pessoa veja as fotos das ofertas
      allow read: if true;
      
      // Permite upload apenas para usuários autenticados no seu app
      allow write: if request.auth != null;
    }
  }
}